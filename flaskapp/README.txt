To run:
1. Install flask -
https://flask.palletsprojects.com/en/1.1.x/installation/#installation

2. cd to /flaskapp

3. run in terminal - 'export FLASK_APP=app.py'

4. to run localhost:
	- 'flask run'
to run in network:
	- 'flask run --host=0.0.0.0'
